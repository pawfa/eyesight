{
  "name": "sight",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/docs",
    "packages/sight-server",
    "packages/sight-cli",
    "packages/sight-playwright"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start": "node packages/sight-server/bin/www"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
